
---
description: Deploy Health Agent to Vercel
---

1. Initialize git repo
```bash
git init
git add .
git commit -m "Initial commit"
```

2. Login to Vercel
```bash
npx vercel login
```

3. Deploy
```bash
npx vercel
```
